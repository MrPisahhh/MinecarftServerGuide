# Minecraft 服务器搭建完全指南（小白向）

> 作者：MakeBy 披萨哈哈哈  
> 适用人群：零基础小白 / 想开服但不知从何下手的服主  
> 最后更新：2026-08-13

---

## 📖 目录
1. [服务器核心选择](#1-服务器核心选择)
2. [模组与插件来源](#2-模组与插件来源)
3. [开发者必备工具](#3-开发者必备工具)
4. [启动脚本编写（.bat）](#4-启动脚本编写bat)
5. [服务端初始化配置](#5-服务端初始化配置)
6. [服务端 配置详解](#6-serverproperties-配置详解)
7. [登录与联机方式](#7-登录与联机方式)
8. [正确关闭服务器](#8-正确关闭服务器)
9. [安装插件 / 模组](#9-安装插件--模组)
10. [权限管理（给玩家分配权限）](#10-权限管理给玩家分配权限)
11. [定期备份（防炸档）](#11-定期备份防炸档)
12. [常见问题与排错（小白救星）](#12-常见问题与排错小白救星)
13. [进阶建议（让服务器更好玩）](#13-进阶建议让服务器更好玩)
14. [结语](#14-结语)

---

## 1. 服务器核心选择

根据你的需求选择核心类型：

| 核心名称 | 用途 | 下载地址 |
|---------|------|---------|
| **Spigot** | 插件服（性能优化版） | [SpigotMC](https://www.spigotmc.org/) |
| **Paper** | 插件服（高性能分支，推荐） | [PaperMC](https://papermc.io/downloads/paper) |
| **Forge** | 模组服（经典） | [Forge 下载站](https://files.minecraftforge.net/net/minecraftforge/forge/) |
| **Fabric** | 模组服（轻量现代） | [Fabric 服务器启动器](https://fabricmc.net/use/server/) |
| **mohist** | 模组+插件（不建议小白学）| [ mohist 下载 ](https://www.mohistmc.cn/) 
> 💡 小白建议：先玩 **Paper** 插件服，简单稳定，社区资源丰富。

---

## 2. 模组与插件来源

- **插件（Plugin）**：
  - [SpigotMC Resources](https://www.spigotmc.org/resources/)
  - [Modrinth (Plugins)](https://modrinth.com/plugin/)
  - [Hangar (PaperMC)](https://hangar.papermc.io/)

- **模组（Mod）**：
  - [Modrinth](https://modrinth.com/)
  - [CurseForge](https://www.curseforge.com/)

---

## 3. 开发者必备工具

- **GitHub**：[www.github.com](https://github.com/) —— 全世界代码仓库，找插件源码或学习必备。
- **加速器（推荐）**：Steam++（Watt Toolkit）[下载地址](https://steampp.net/)，访问 GitHub 和国外网站更流畅。
- **IDE（编程环境）**：
  - 零基础小白 → 推荐 **Trae AI**：[https://www.trae.cn/](https://www.trae.cn/)（智能辅助，上手快）
  - 熟悉 Java 的老手 → 推荐 **IntelliJ IDEA**：[下载页](https://www.jetbrains.com/zh-cn/idea/download/?section=windows)
- **Java 环境**（必须！）：
  - 云盘下载（含各版本 JDK）：[点击这里](https://1838711787.share.123pan.cn/123pan/SqdGTd-geptv?pwd=MrPS) 密码：`MrPS`
  - 推荐使用 **JDK 17 或 21**（对应 Minecraft 1.18+ / 1.20+）。

---

## 4. 启动脚本编写（.bat）

**步骤 1**：新建一个文本文档（`.txt`），将文件名改为 `Start.bat`（注意扩展名要变成 `.bat`）。  
**步骤 2**：右键编辑，粘贴以下模板：

```bat
@echo off
"你的Java安装路径\bin\java.exe" -Xms4G -Xmx6G -jar server.jar nogui
pause
```

**参数说明**：
- `title`：窗口标题，随意写。
- `-Xms4G`：最小分配内存（单位 G），建议 2~4G。
- `-Xmx6G`：最大分配内存（单位 G），根据服务器内存调整。
- `-jar server.jar`：核心文件名，必须与你下载的 jar 名称一致。
- `nogui`：不显示 Minecraft 自带 GUI（推荐），若想显示可去掉。
- `pause`：窗口停留，方便查看报错信息。

> 示例（实际路径请改成你自己的）：
```bat
@echo off
title Minecraft Server BY MrPisahhh 30002
"C:\Java\jdk-21\bin\java.exe" -Xms4G -Xmx6G -jar server.jar nogui
pause
```

---

## 5. 服务端初始化配置

1. 将核心文件（如 `server.jar`）和启动脚本 `Start.bat` **放在同一个文件夹**。
2. **双击运行 `Start.bat`**，会生成一堆文件和文件夹（包括 `eula.txt`）。
3. 打开 `eula.txt`，将 `eula=false` 改为 `eula=true` 并保存（表示同意 [Minecraft EULA](https://aka.ms/MinecraftEULA)）。
4. **再次双击 `Start.bat`**，等待加载完成，直到出现：
   ```
   [时间 INFO]: Done (16.302s)! For help, type "help"
   ```
   说明服务器成功启动！🎉

---

## 6. server.properties 配置详解

这是服务器的核心配置文件，用记事本打开即可编辑。

> 📌 **重要**：所有以 `#` 开头的行都是注释，不会生效，仅作说明。以下参数按需修改。

| 参数名 | 说明 | 建议值 |
|--------|------|--------|
| `allow-flight` | 是否允许生存模式飞行 | `false`（防作弊） |
| `allow-nether` | 是否开启下界 | `true` |
| `broadcast-console-to-ops` | 控制台信息是否发给 OP | `true` |
| `difficulty` | 难度 | `easy` / `normal` / `hard` |
| `enable-command-block` | 是否允许命令方块 | `true`（若需要） |
| `enable-rcon` | 是否开启远程控制 | `false`（新手不建议） |
| `enforce-whitelist` | 是否强制白名单 | `false`（基友服可关闭） |
| `gamemode` | 默认游戏模式 | `survival` / `creative` |
| `generate-structures` | 是否生成结构（村庄等） | `true` |
| `hardcore` | 是否极限模式 | `false` |
| `level-name` | 世界文件夹名称 | `world`（默认） |
| `level-seed` | 世界种子 | 留空随机 |
| `max-players` | 最大玩家数 | 根据服务器性能设置 |
| `motd` | 服务器标语（显示在多人游戏列表） | `欢迎来到我的服务器~` |
| `online-mode` | 是否正版验证 | `true`（正版服） / `false`（离线服） |
| `pvp` | 是否允许 PVP | `true` |
| `server-port` | 端口号 | `25565`（默认） |
| `view-distance` | 客户端可视距离（区块） | `6~10`（调低可优化性能） |
| `simulation-distance` | 模拟距离（区块） | `6~8` |
| `spawn-protection` | 出生点保护半径（格） | `16` |

---

## 7. 登录与联机方式

- **本地登录**：输入 `127.0.0.1:端口`（例如 `127.0.0.1:25565`）。
- **局域网好友联机**：
  - 获取你的内网 IP（`ipconfig` 查看），好友输入 `你的内网IP:端口`。
- **外网好友联机（公网 IP）**：
  - 直接输入你的公网 IP + 端口（需在路由器设置端口转发）。
- **内网穿透（推荐无公网 IP 用户）**：
  - 使用 SakuraFrp、Ngrok 等穿透工具，选择 TCP 协议。
  - 目标 IP 填写 `0.0.0.0` 或 `127.0.0.1`，目标端口填写 `server-port`。
  - 穿透后会生成一个外网地址，发给好友即可。

---

## 8. 正确关闭服务器

**千万不要直接点窗口的 ×**，否则可能损坏存档！  
正确方法：在服务器控制台输入 `stop` 并回车，等待保存完毕后窗口自动关闭。

---

## 9. 安装插件 / 模组

### 🔌 插件（Paper / Spigot）
1. 从上面提到的网站下载 `.jar` 插件文件。
2. 放入服务器文件夹内的 **`plugins`** 目录（没有就自己新建）。
3. 重启服务器（完全停止再启动），插件会自动加载。
4. 插件的配置文件生成在 `plugins/插件名/` 下，可自行修改。
5. 在cmd[命令行]中输入plugins回车可以看到服务器是否启用了插件[绿色就是已经启用/红色就是没有启用/没有说明放错了]

### 🧩 模组（Forge / Fabric）
- Forge：将模组 `.jar` 放入 **`mods`** 文件夹，启动即可。
- Fabric：同样放入 `mods`，但需提前安装 Fabric API 等前置模组（查看模组页面要求）。
- ⚠️ 注意：模组服要求客户端也安装相同的模组（客户端服务端都要有）。

---

## 10. 权限管理（给玩家分配权限）

推荐使用 **LuckPerms** 插件（支持所有核心）。

1. 下载 LuckPerms 放入 `plugins`，重启。
2. 在控制台输入：
   - `/lp creategroup vip` —— 创建 VIP 组
   - `/lp group vip permission set essentials.fly` —— 给该组添加飞行权限（需安装 EssentialsX）
   - `/lp user 玩家名 parent add vip` —— 将玩家加入 VIP 组
3. 更多指令请参考 [LuckPerms Wiki](https://luckperms.net/wiki)。

> 小型基友服可直接用 `/op 玩家名` 给予最高管理员权限，但请谨慎。这类玩家可以执行/stop 来关闭服务器

---

## 11. 定期备份（防炸档）

- **手动备份**：关闭服务器后，复制整个服务器文件夹或至少 `world` 文件夹，重命名为“备份_日期”。
- **自动备份**：
  - 使用插件 [DriveBackupV2](https://www.spigotmc.org/resources/drivebackupv2.100389/) 或 [Backup](https://www.spigotmc.org/resources/backup.111542/)。
  - 或利用系统任务计划定时执行 `.bat` 脚本复制文件夹。

---

## 12. 常见问题与排错（小白救星）

| 问题现象 | 解决方案 |
|---------|----------|
| **双击 .bat 闪退** | 检查 Java 路径是否正确；检查内存参数是否超出实际内存；检查 `server.jar` 文件名是否匹配。 |
| **提示 “Java not found”** | 在 .bat 中使用绝对路径（如 `"C:\Program Files\Java\jdk-21\bin\java.exe"`）。 |
| **外部玩家连接不上** | 检查防火墙是否开放端口；检查路由器端口转发；如果是内网穿透，确认穿透地址和端口正确。 |
| **报错 “Failed to bind to port”** | 端口被占用，关闭其他服务器或修改 `server-port`。 |
| **玩家进服提示版本不匹配** | 确保服务器核心版本和客户端版本完全一致。 |
| **世界加载异常或出生点卡死** | 尝试删除 `world` 文件夹重新生成（注意备份），或检查 `level-type` 设置。 |

---

## 13. 进阶建议（让服务器更好玩）

- **性能优化**：降低 `view-distance` 和 `simulation-distance`，关闭不用的世界（如 `allow-nether=false`）。
- **定时重启**：使用 `RestartReboot` 插件或 Windows 任务计划每天凌晨自动重启服务器，释放内存。
- **经济与商店**：安装 `PlayerPoint` + `Vault` + `ChestShop` 打造经济系统。
- **反作弊**：`AntiCheatReloaded` 或 `Matrix`，但注意性能开销。[新手不建议使用]
- **清理掉落物**：定期执行 `/kill @e[type=!player]` 或使用清理插件（如 `ClearLag`）。
- **开启 RC 远程管理**（高级）：设置 `enable-rcon=true` 并配置密码，可用第三方工具远程管理。

---
## 14.更多功能社区

- **[MineBBS](https://www.minebbs.com/)** 我的世界大社区，有Java/基岩的mod/插件
- **[Github](https://github.com/)** Minecarft众多资源如PCL2和其他的开源文件
- **[NameMC](https://www.namemc.com)** 正版玩家皮肤下载
- **[我的世界开服器MSL](https://www.mslmc.cn/)** 更简单的开服【小白从这开始学更好】
- 等等

## 15. 结语

至此，你已掌握了从零搭建 Minecraft 服务器的全部基础技能。  
开服路上肯定会遇到各种报错，别怕 —— 99% 的问题都能通过搜索引擎或ai程序[doubao/deepseek/qwen/chatGPT]找到答案。  
如果不想折腾命令行，也可尝试免费面板服（如 [简幻欢](https://simpfun.cn/auth?type=register&code=300511364)），但自己搭建的服务器更具掌控感和成就感。

**祝你的服务器人气爆棚，和小伙伴们玩得开心！** 🎮🍕

---

> 📝 本文档由  披萨哈哈哈 编写，欢迎分享和修改，但请保留原作者信息。  
> 如有疑问或补充，欢迎提交 Issue 或 Q3590018649再或者mrpisahhh@outlook.com。